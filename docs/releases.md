# Releases

Keep notable changes under `## Unreleased` in `CHANGELOG.md`. Move them into a version heading only when tagging.

Mark `alpha`, `beta`, and `rc` GitHub Releases as pre-releases. Do not publish a release until tests and notes match the tagged code.
